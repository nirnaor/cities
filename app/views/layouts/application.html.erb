<!DOCTYPE html>
<html>
<head>
  <title>Cities</title>
  <%= stylesheet_link_tag    "application", :media => "all" %>
  <%= javascript_include_tag "application" %>
  <%= csrf_meta_tags %>
</head>
<body>

<div class="navbar">
  <div class="navbar-inner">
    <a class="brand" href="#">Title</a>
    <ul class="nav">
      <li class="<%= 'active' if current_page?(cities_path) %>">
        <%= link_to 'All Cities', cities_path %>
      </li>
      <li class="<%= 'active' if current_page?(choice_check_path ) %>">
        <%= link_to "Prioritise", choice_check_path %>
      </li>
    </ul>
  </div>
</div>

<%= yield %>

</body>
</html>
